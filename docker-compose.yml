services:
  app:
    image: busybox:1.36
    command: ["sh", "-c", "while true; do echo \"lumo compose test ${LUMO_INSTANCE_ID:-test}\"; sleep 3600; done"]
    environment:
      LUMO_INSTANCE_ID: ${LUMO_INSTANCE_ID:-test}
      LUMO_DOMAIN: ${LUMO_DOMAIN:-localhost}
      PORT: ${PORT:-8080}
